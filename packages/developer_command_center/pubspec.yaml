name: developer_command_center
description: Developer Command Center - Advanced debugging and analytics dashboard
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.8.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^1.0.0
  firebase_core: ^4.0.0
  firebase_analytics: ^12.0.0
  package_info_plus: ^8.3.1
  network_logger:
    path: ../network_logger

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true