part of '../indicator.dart';

class _IndicatorLine extends StatelessWidget {
  const _IndicatorLine();

  @override
  Widget build(BuildContext context) =>
      Divider(color: context.colorScheme.secondary, height: 0.01);
}
